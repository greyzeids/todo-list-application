import React from "react";
import TodoList from "./TodoList";

export const Home = () => {
    return (
        <div className="FormBack">
            <TodoList />
        </div>
    );
};

export default Home;
